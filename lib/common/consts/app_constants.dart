import 'package:flutter/material.dart';

class AppConstants {
  static const fallbackLocale = Locale('ru');
  static final supportedLocales = [
    const Locale('ru'),
  ];
  static const translationsPath = 'assets/translations';
  static const defaultLocale = 'ru_RU';

  static const appName = 'Weather';
}
